Split backend and frontend into separate folders,in the same repo or using monorepo.
Backend folder is for scheduling and sending.
Frontend is for talking to the API endpoints and displaying the data.